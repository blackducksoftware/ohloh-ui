%p.settings-page-description.forum-breadcrumb
  = link_to t('forums.index.forums'), forums_path
  %span.account-basics-sep ›
  = link_to h(forum.name), forum_path(forum)
= render partial: '/forums/notice'
