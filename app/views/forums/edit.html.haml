%h1.margin_bottom_20
  %span= link_to t('.forums'), forums_url
.clear

.row
  .col-md-8.float_left
    %h2= t('.edit_forum')

.well.col-md-11
  .col-md-7
    = form_for @forum do |f|
      = render partial: 'form', locals: { f: f }
      = f.submit t('.save_forum'), class: 'btn btn-small btn-primary'
      = link_to t('.cancel'), forum_path(@forum), class: 'btn btn-small'
