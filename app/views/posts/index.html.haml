/ TODO: Implement Search/Filter etc. for this page. New Ticket.
- if @account
  %h2 Posts
  = render partial: 'shared/search_dingus', locals: { collection: @posts, sort_option_name: :posts }
- else
  %h1.margin_bottom_20
    = link_to t('.forums'), forums_url
    \:
    Posts
    = render partial: 'shared/search_dingus', locals: { collection: @posts, sort_option_name: :posts }
  .clear_left &nbsp;

- if @posts.any?
  / = render :partial => 'searchables/post', :collection => @posts
  = will_paginate @posts
/ - elsif current_objects.query_parser.actual_query.nil?
/   .no-posts
/     No posts available to display.
/ - elsif current_objects.total_entries == 0
/   - no_search_match
