= f.text_area :body, class: 'reply-textarea', placeholder: t('.write_reply')
- error_tag @post, :body
- unless current_user_is_admin?
  = render partial: 'shared/captcha'
  - error_tag @post, :captcha
.reply-form-actions
  = f.submit t('.submit'), class: 'reply-submit-btn'
