%div{ id: "post_#{post.id}" }
  / TODO: Figure out how the coloring works for post. Colors do not alternate properly even in ohloh app.
  .well{ style: "#{current_user_is_admin? ? 'background: #D5E3E9;' : ''}" }
    / TODO: Blue cloth was used for rendering the post body. ohloh application_helper.rb format_text
    / Blue cloth is outdated. Find another gem to fix this.
    = strip_tags(post.body)
    .clearfix.margin_top_10
      .col-md-3.pull-right
        .pull-left
          %a= avatar_img_for post.account, 42
        .pull-left.margin_left_5
          %a.avatar_name{ href: account_path(post.account) }
            = truncate(h(post.account.name), length: 18)
          %small.date.avatar_name
            = time_ago_in_words(post.created_at)
      - if (current_user.id == post.account_id) || (current_user_is_admin?)
        %p= link_to t('.edit_post'), edit_topic_post_path(post.topic, post), class: 'btn btn-mini btn-info'
      .clear
.row
  &nbsp;
