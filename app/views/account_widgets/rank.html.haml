:ruby
  widget ||= @widget
  bbc ||= false
  account = widget.account
  ref ||= widget.class
  # TODO: image = rank_account_account_widgets_url(account, format: 'gif', ref: ref)
  image = image_url('icons/sm_laurel_9.png')

= render partial: 'bbc_or_js', locals: { bbc: bbc, widget: widget, url: account_url(account, ref: ref), image: image }
