- content_for(:html_title) { t('.page_title', name: @project.name) }

.settings-page-header
  .settings-page-header-row
    .settings-page-icon
      %i.fa.fa-pencil
    %h2.settings-page-h2
      = t('.page_header')

.duplicate-form-layout
  .oh-card
    = form_for @duplicate, url: project_duplicate_path(@project, @duplicate), html: { method: 'put' } do |form|
      = render partial: 'fields', locals: { form: form, button_text: t('.button_text') }

= render partial: 'instructions'
