slave.jobs.failed.since(get_window).each_with_index do |job, i|
  br '' if i != 0 && i % 4 == 0
  a href: admin_job_path(job), style:"text-decoration:none;" do
    status_tag("#{job.class.to_s} (#{job.id})", :no, title: job.current_step_at)
  end
end
