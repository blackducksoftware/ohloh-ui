- page_context[:title] = project_pages_title t('.page_title')

%h2.float_left
  = link_to t(:settings), settings_project_path(@parent)
  &#58;
  = link_to t(:managers_link_name), parent_managers_path
  &#58;
  #{t(:edit)}

.clear
.col-md-8
  .well
    = form_for(@manage, url: parent_manager_path, html: { method: :put }) do |form|
      = render partial: 'form', locals: { form: form }

.clear
= render partial: 'about'
