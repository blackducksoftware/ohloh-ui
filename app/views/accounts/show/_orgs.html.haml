- # NOTE: replaces affilations_for helper in accounts_helper
= pluralize(size, "organization", "organizations")
%span.right_sm
  = t('.including')
  %br
  - if links
    = orgs.first(3).map(&:names).to_sentence
  - else
    = orgs.first(3).map { |org| link_to org, organization_path(org) }.to_sentence
