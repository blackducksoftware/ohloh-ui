= pluralize(orgs.size, 'organization', 'organizations')
%span.right_sm
  = t('.including')
  %br
  - if links
    = orgs.first(3).map(&:name).to_sentence
  - else
    != orgs.first(3).map { |org| link_to org.name, organization_path(org) }.to_sentence
