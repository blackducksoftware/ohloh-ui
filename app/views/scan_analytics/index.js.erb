$("#scan_data").on("ajax:before", function() {
  $('.overlay-loader').show();
});
$("#scan_data").html("<%= escape_javascript(render partial: 'index') %>");
$('.chzn-select').chosen();
App.ScanAnalytics.init();
