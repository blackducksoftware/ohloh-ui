%p= t('mailers.greeting')
%h4!= t('.body', destroyer: @manage.destroyer.name, name: @manage.account.name)
%p= t('mailers.signature')
%hr
%p{ style: 'color:777' }= t('manage_mailer.privacy_to_managers')
