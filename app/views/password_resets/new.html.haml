- content_for(:html_title) { t('password_resets.title') }

.col-md-12
  %h3= t('password_resets.title')

  - if flash[:notice]
    .alert.alert-info= flash[:notice]

  .row
    .col-md-7
      .well
        = form_for @password_reset do |f|
          %fieldset
            .form-group
              %label.control-label.required= t('email_address')
              .controls
                = f.text_field :email
                - error_tag @password_reset, :email
                %p.help-block= t('.enter_email_address')

            .actions
              = f.submit t('.submit_text'), class: 'btn btn-primary'
