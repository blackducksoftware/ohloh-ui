- key, klass = { error: 'alert-error', notice: 'alert-info', success: 'alert-success' }.find { |k, _v| flash[k] }
- if key
  #flash-msg
    .margin_top_20.alert{class: klass}
      %a.close{ href: '#', 'data-dismiss' => 'alert' }= '×'
      :ruby
        icon = case key
               when :error
                 'exclamation'
               when :notice
                 'info'
               when :success
                 'ok'
               else
                 'warning'
               end
      %i{ class: "icon-#{icon}-sign" }
      != flash[key]
